{
  "name": "fertilizer-commerce",
  "version": "1.0.0",
  "private": true,
  "description": "Nền tảng TMĐT phân bón dành cho thị trường Việt Nam",
  "workspaces": [
    "packages/*",
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "build:packages": "npm run --workspace=@phanbonshop/shared-types build && npm run --workspace=@phanbonshop/shared-utils build && npm run --workspace=@phanbonshop/logger build && npm run --workspace=@phanbonshop/config build",
    "prisma:generate": "npm run --workspaces --if-present prisma:generate",
    "prisma:validate": "node scripts/prisma-validate.mjs",
    "prisma:db:push": "npm run --workspaces --if-present prisma:db:push",
    "migrate:deploy": "node scripts/migrate-deploy.mjs",
    "postinstall": "npm run prisma:generate",
    "pretypecheck": "npm run build:packages && npm run prisma:generate",
    "typecheck": "npm run --workspaces --if-present typecheck",
    "prebuild": "npm run build:packages && npm run prisma:generate",
    "build": "npm run --workspaces --if-present build",
    "lint": "npm run --workspaces --if-present lint",
    "test": "npm run --workspaces --if-present test && node scripts/test-backup-restore.mjs && node scripts/test-production-hardening.mjs",
    "test:integration": "npm run --workspace=@phanbonshop/auth-service test:integration && npm run --workspace=@phanbonshop/inventory-service test:integration && npm run --workspace=@phanbonshop/order-service test:integration",
    "test:backup": "node scripts/test-backup-restore.mjs",
    "test:local:cod": "node scripts/test-local-cod.mjs",
    "test:hardening": "node scripts/test-production-hardening.mjs",
    "test:payment": "node services/order-service/test/payment.architecture.test.mjs",
    "test:momo": "node services/order-service/test/momo.sandbox.test.mjs",
    "backup": "bash scripts/backup-databases.sh",
    "backup:win": "powershell -ExecutionPolicy Bypass -File scripts/backup-databases.ps1",
    "restore:win": "powershell -ExecutionPolicy Bypass -File scripts/restore-databases.ps1",
    "seed": "npm run --workspaces --if-present seed",
    "dev": "npm run --workspaces --if-present dev",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  }
}
